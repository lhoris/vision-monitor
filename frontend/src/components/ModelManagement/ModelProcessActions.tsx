import type { ModelControlAction, ModelProcess } from '@/types/modelManagement'

export function ModelProcessActions({ process, busy, onControl, onSettings, onLogs }: { process: ModelProcess; busy: boolean; onControl: (action: ModelControlAction) => void; onSettings: () => void; onLogs: () => void }) {
  return <div className="flex flex-wrap items-center gap-1"><button type="button" className="rounded border border-slate-300 px-2 py-1 text-xs hover:bg-slate-100 disabled:opacity-50 dark:border-slate-600 dark:hover:bg-slate-700" disabled={busy || process.processStatus === 'running'} onClick={() => onControl('start')}>시작</button><button type="button" className="rounded border border-slate-300 px-2 py-1 text-xs hover:bg-slate-100 disabled:opacity-50 dark:border-slate-600 dark:hover:bg-slate-700" disabled={busy || process.processStatus === 'stopped'} onClick={() => onControl('stop')}>정지</button><button type="button" className="rounded border border-amber-300 px-2 py-1 text-xs text-amber-700 hover:bg-amber-50 disabled:opacity-50 dark:border-amber-700 dark:text-amber-300" disabled={busy} onClick={() => onControl('restart')}>재시작</button><button type="button" className="rounded border border-blue-300 px-2 py-1 text-xs text-blue-700 hover:bg-blue-50 dark:border-blue-700 dark:text-blue-300" onClick={onSettings}>설정</button><button type="button" className="rounded border border-slate-300 px-2 py-1 text-xs hover:bg-slate-100 dark:border-slate-600 dark:hover:bg-slate-700" onClick={onLogs}>로그</button></div>
}
