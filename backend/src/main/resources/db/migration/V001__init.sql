-- Baseline marker only.
-- Legacy monitoring tables were removed from the baseline schema because the MVP
-- keeps camera, stream, event, recording, and alert workflows on frontend mocks
-- until the final M26 backend contracts are defined.
SELECT 1;
