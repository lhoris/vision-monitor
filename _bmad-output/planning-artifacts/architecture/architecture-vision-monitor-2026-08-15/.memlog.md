---
scope: 카메라 집중 보기 / 확대 보기 brownfield architecture
purpose: feature build substrate and implementation-facing architecture document
altitude: feature
updated: 2026-08-15T11:33
---

- (decision) Live streamUrl and recording playbackUrl are separate API contracts because their lifecycle, time range, auth expiry, player behavior, and error states differ.
- (constraint) Vision Monitor does not own RTSP ingest, AI inference, media distribution, server-side overlay, transcoding, or original recording storage; external VMS/AI/Media Server provides browser-playable live and playback URLs plus event metadata.
- (decision) Spring Boot remains the persistence and contract boundary for cameras, events, recordings, alerts, layout, and metadata, but does not proxy or generate media streams in MVP.
- (decision) Live streamUrl and recording playbackUrl are separate API contracts because their lifecycle, time range, auth expiry, player behavior, and error states differ.
- (event) spine and brownfield architecture drafted and finalized for camera focus view
